# A组 MVP Demo 技术方案

## 项目定位

项目名称：Renaissance AI 简历优化助手  
A组职责：产品开发与运营  
本阶段目标：完成可运行的 MVP 原型，并录制产品功能演示 Demo。

本方案只覆盖 A 组需要交付的产品原型和演示闭环，不实现 B 组负责的多智能体项目管理系统、BBS 黑板系统、风险调度智能体等底层架构。

---

## 一、A组交付边界

### 必须交付

1. 可运行的 MVP 原型
   - 简历文本输入
   - 目标岗位 JD 输入
   - 一键优化
   - 优化结果展示
   - 匹配度评分
   - 关键词命中展示
   - Before / After 对比
   - 导出 PDF 演示能力

2. 产品功能演示 Demo
   - 展示完整黄金路径：输入简历和 JD -> AI 诊断 -> AI 优化 -> 评分和对比 -> 导出
   - 支持稳定的“剧本模式”，避免现场因为网络、API 额度或响应延迟翻车

3. 答辩说明材料
   - 说明 A 组 MVP 如何快速验证用户体验
   - 说明后续如何与 B 组智能体架构对接

### 暂不实现

1. 登录注册、会员体系、支付系统
2. 数据库与长期用户档案存储
3. 复杂 PDF/DOCX 解析
4. 多岗位批量生成
5. 企业端 HR 管理后台
6. BBS 多智能体调度系统

这些能力属于正式产品或 B 组架构集成阶段，不应放进 A 组当前 MVP 的开发范围。

---

## 二、MVP 核心体验路径

### 1. 输入

用户在页面左侧输入旧版简历文本，在右侧输入目标岗位 JD。

为了保证 Demo 节奏，页面提供“填入样例”按钮，演示时可一键加载提前准备好的简历和岗位描述。

### 2. 处理

点击“开始优化”后，系统展示四个阶段：

1. 解析简历经历
2. 提取 JD 关键词
3. 应用 STAR 法则重写
4. 计算岗位匹配度

真实逻辑由后端代理调用 DeepSeek API 完成；演示时也可以打开剧本模式，使用本地 Mock 数据模拟 AI 流式生成。

### 3. 输出

页面输出三类结果：

1. 优化后的简历
   - 使用更专业的表达
   - 补充行动和结果
   - 加入可量化成果
   - 强化与 JD 的关键词匹配

2. 数据化看板
   - 综合匹配度
   - 关键词命中情况
   - 优化前后竞争力雷达图

3. Before / After 对比
   - 左侧显示原始表达
   - 右侧显示优化表达
   - 高亮关键词、数字化成果和专业动作

---

## 三、技术选型

### 原型实现

本地 Node.js 服务 + 原生前端页面。

选择理由：

1. 不依赖复杂构建工具，启动速度快
2. 不需要数据库，适合课堂 Demo
3. API Key 留在服务端，不暴露到浏览器
4. 出问题时可以立即切换剧本模式
5. 便于录屏和现场演示

### AI 模型

使用 DeepSeek API。

默认模型：`deepseek-v4-flash`  
备用模型：`deepseek-v4-pro`

DeepSeek API 使用 OpenAI 兼容格式，后端通过 `https://api.deepseek.com/chat/completions` 发起请求。

### 安全原则

1. API Key 不写入前端代码
2. API Key 不提交到仓库
3. 本地通过环境变量或 `.env.local` 提供
4. Demo 完成后建议轮换一次 API Key

---

## 四、后端接口设计

### `/api/optimize`

请求方式：`POST`

请求字段：

```json
{
  "resume": "用户旧版简历文本",
  "jobDescription": "目标岗位JD",
  "demoMode": true
}
```

响应方式：Server-Sent Events 风格的流式事件。

事件类型：

1. `phase`：当前处理阶段
2. `result`：结构化优化结果
3. `chunk`：逐字输出的简历文本
4. `warning`：真实 API 不可用时的兜底提示
5. `done`：处理完成

### DeepSeek Prompt 目标

后端要求模型返回 JSON，结构如下：

```json
{
  "optimizedResume": "优化后的Markdown简历",
  "matchScore": 88,
  "keywordHits": [
    { "keyword": "用户增长", "before": false, "after": true }
  ],
  "radar": [
    { "label": "关键词匹配", "before": 45, "after": 88 }
  ],
  "sections": [
    {
      "original": "负责社群运营",
      "optimized": "运营500人求职社群，设计转化路径，使活动报名率提升18%",
      "reasoning": "补充规模、动作和结果，增强岗位相关性"
    }
  ],
  "suggestions": [
    "补充真实数据后可进一步提升可信度"
  ]
}
```

---

## 五、前端页面设计

页面直接展示产品本体，不做营销落地页。

### 区域结构

1. 顶部控制栏
   - 项目名称
   - Demo 模式开关
   - 填入样例
   - 开始优化
   - 导出 PDF

2. 输入区
   - 简历输入框
   - JD 输入框

3. 处理中枢
   - 四阶段进度
   - 综合匹配分
   - 关键词命中
   - 雷达图

4. 输出区
   - 流式优化结果
   - Before / After 对比
   - 修改理由

### 视觉风格

整体风格：深色专业 SaaS 工具界面  
主色：石墨黑、青绿色、暖金色、浅蓝色  
目标：看起来像一个已经能给用户试用的产品，而不是 PPT 草图。

---

## 六、演示脚本

### 1分钟 Demo 流程

1. 打开页面，展示 Renaissance AI 主界面
2. 点击“填入样例”
3. 检查左侧简历和右侧 JD 已填入
4. 点击“开始优化”
5. 展示四阶段处理动画
6. 展示优化后的简历逐字出现
7. 展示匹配度从低到高变化
8. 展示关键词命中和雷达图
9. 展示 Before / After 对比
10. 点击“导出 PDF”，进入浏览器打印保存流程

### 答辩话术

“A组当前交付的是 MVP 原型，核心目标不是一次性做完整商业系统，而是快速验证用户体验。我们用 DeepSeek API 实现简历诊断、JD 匹配和 STAR 重写，用剧本模式保障现场演示稳定。正式版本中，这个优化请求可以接入 B 组的 BBS 多智能体系统，由 JD 解析 Agent、简历诊断 Agent、重写 Agent 和风险校验 Agent 协同完成。A组 MVP 的输入输出结构可以直接作为后续集成接口。”

---

## 七、风险与兜底

| 风险 | 影响 | 兜底方案 |
|---|---|---|
| API Key 无效 | 无法真实调用模型 | 使用剧本模式完成演示 |
| API 响应慢 | 现场等待过久 | 前端先展示阶段动画，超时后自动兜底 |
| 网络断开 | 无法连接 DeepSeek | 剧本模式完全本地可用 |
| 模型返回格式不稳定 | 前端解析失败 | 后端做 JSON 解析和 Mock 回退 |
| PDF 导出排版复杂 | 影响演示节奏 | 使用浏览器打印保存为 PDF |

---

## 八、当前完成标准

当满足以下条件，即视为 A 组 MVP 完成：

1. 页面可以本地打开并运行
2. 可以填入简历和 JD
3. 可以点击开始优化
4. 可以看到阶段动画
5. 可以看到优化结果流式输出
6. 可以看到匹配度、关键词、雷达图
7. 可以看到 Before / After 对比
8. 可以导出或打印 PDF
9. 即使没有 API，也能用剧本模式完成完整演示

---

## 九、运行方式

1. 安装 Node.js
2. 在项目目录创建 `.env.local`
3. 写入：

```env
DEEPSEEK_API_KEY=你的DeepSeek API Key
DEEPSEEK_MODEL=deepseek-v4-flash
PORT=3000
```

4. 启动：

```bash
npm start
```

5. 浏览器访问：

```text
http://localhost:3000
```

如果不配置 API Key，页面仍可使用剧本模式完成演示。
